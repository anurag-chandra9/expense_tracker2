{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Expense Tracker Django Application",
  "scripts": {
    "build": "sh build_files.sh"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
